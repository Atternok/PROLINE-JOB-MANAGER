'use client';

// We import the component we moved earlier
import { BuildingsDashboard } from '../components/BuildingsDashboard';

export default function HomePage() {
  return (
    <main>
      <BuildingsDashboard />
    </main>
  );
}

